/*let login = [];
function fetchLogin() {
  const post = fetch("http://localhost:5678/api/users/login", {
    METHOD: "POST",
    body: JSON.stringify(data),
  });

  return post.json();
}

export { fetchLogin, login };
*/
